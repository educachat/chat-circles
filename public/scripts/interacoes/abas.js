function Abas(elem) {
  this.init(elem);
}

Abas.prototype.init = (elem) => {
  let el = document.querySelectorAll(elem);
}

Abas.prototype.setActive = (elem) => {
  let el = document.querySelectorAll(elem);
  let elConteudo = document.querySelectorAll('.aba-conteudo');
  console.log(elConteudo);
}